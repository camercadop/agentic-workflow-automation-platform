# API Schemas

Pydantic v2 request/response models for API validation and serialization.

| Module | Models |
|--------|--------|
| `workflows.py` | Workflow create/read schemas, node and edge definitions |
| `plugins.py` | Plugin metadata response schemas |
| `executions.py` | Execution status, results, and history schemas |
